# Python-Abhängigkeiten der Zusatzdienste.
#
#   Texterkennung (services/classifier)  → easyocr, torch, pillow, numpy
#   Bildgenerierung (services/imagegen)  → mflux (nur Apple Silicon)
#   Eigenschaftsvorhersage               → scikit-learn, joblib
#
# Installation über services/setup.sh — die Auswahl richtet sich danach,
# welche Module in Betrieb sind. Alles läuft offline; nichts ruft nach außen.

# --- Texterkennung -----------------------------------------------------------
easyocr>=1.7
torch>=2.2
torchvision>=0.17
pillow>=10
numpy>=1.26

# --- Eigenschaftsvorhersage --------------------------------------------------
scikit-learn>=1.4
joblib>=1.3

# --- Bildgenerierung (nur macOS mit Apple Silicon) ---------------------------
# Auf anderen Plattformen weglassen; das Modul lässt sich im Adminbereich
# abschalten, dann fehlt nichts.
# mflux>=0.4 ; sys_platform == 'darwin' and platform_machine == 'arm64'
